apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: database-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/YOUR_USERNAME/gitops-argocd-tp.git
    targetRevision: HEAD
    path: charts/database
    helm:
      valueFiles:
        - ../../envs/dev/database-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
